apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: openshift-gitops
commonLabels:
  app: quarkus-producer
  kind: application
  master: openshift-gitops

resources:
  - project.yaml
  - application.yaml
